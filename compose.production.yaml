services:
  backend:
    env_file: .prod-env
    image: ghcr.io/randomtanne/yac/backend:${BACKEND_TAG:?error}
    restart: always

  frontend:
    env_file: .prod-env
    image: ghcr.io/randomtanne/yac/frontend:${FRONTEND_TAG:?error}
    restart: always

  caddy:
    env_file: .prod-env
    image: ghcr.io/randomtanne/yac/caddy:${CADDY_TAG:?error}
    restart: always

  postgres:
    env_file: .prod-env
